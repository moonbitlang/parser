import {
  "moonbitlang/lexer/basic",
}

formatter(ignore: [ "tokens.mbt" ])

rule(
  name: "moonyacc",
  command: "moonx moonbitlang/yacc@0.7.22 --mode only-tokens $input -o $output",
)

dev_build(rule: "moonyacc", input: "parser.mbty", output: "tokens.mbt")
