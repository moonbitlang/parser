{
  "pre-build": [
    {
      "command": "$mod_dir/.mooncakes/moonbitlang/yacc/moonyacc --mode only-tokens $input -o $output && moonfmt -w $output && moonfmt -w $output",
      "input": "../../parsing/parser/parser.mbty",
      "output": "tokens.mbt"
    }
  ],
  "import": [
    {
      "path": "moonbitlang/parser/parsing/syntax_types",
      "alias": "ast_types"
    }
  ]
}
